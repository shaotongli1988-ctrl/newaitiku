interface:
  display_name: "验收标准构建"
  short_description: "把模糊需求转成可测试、可验收、可追踪的验收条件"
  default_prompt: "使用 $acceptance-criteria-builder 为当前需求生成验收标准、UAT 场景和 Given/When/Then 清单。"
policy:
  allow_implicit_invocation: true
