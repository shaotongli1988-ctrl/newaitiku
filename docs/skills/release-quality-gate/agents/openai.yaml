interface:
  display_name: "发布质量门禁"
  short_description: "聚合测试、缺陷、覆盖率与 UAT 结论，判断是否允许上线"
  default_prompt: "使用 $release-quality-gate 汇总当前版本的测试与缺陷状态，输出是否允许上线和阻断项。"
policy:
  allow_implicit_invocation: true
