# 老师双岗权限配置核对清单（2026-03-28）

## 环境与配置核对

- 目标环境：dev / test / prod。
- 配置核对项：
  - 鉴权密钥与 token 过期时间保持一致。
  - 角色权限映射键保持一致（`recruit`、`teach`、`recruit+teach`）。
  - 路由权限白名单与后端权限键一致。
- 环境变量：本次不新增 `.env` 项，仅复核既有变量。

## 差异记录

- dev：用于联调验证。
- test：用于 UAT 验收。
- prod：按既有发布流程上线，配置与 test 对齐。

## 验证

- 配置生效后，超管端开通岗位，教师重新登录即可生效。
- 单岗账号和双岗账号均完成权限与数据范围校验。
