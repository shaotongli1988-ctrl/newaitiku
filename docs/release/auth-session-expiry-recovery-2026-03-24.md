# 登录态失效恢复说明

## 交付总览

- 将“登录态已失效”和“仅支持登录态访问”的后端响应从普通权限拒绝调整为未认证状态。
- 保持前端既有 `401` 恢复流程，让页面在登录态过期时自动清理本地 token 并跳回登录页。

## 测试说明

- 更新后端鉴权回归用例，覆盖 token 失效和登出后的控制台访问场景。
- 更新前端请求层错误码映射用例，覆盖 `AUTH_UNAUTHORIZED` 文案。
