# 发布前检查与部署证据（老师双岗权限，2026-03-28）

## 发布与上线目标

- 版本目标：老师双岗权限（招生/教学）与超管开通同步生效。
- 上线范围：题库后端鉴权、超管用户管理、教师首页权限显隐。

## 部署证据

- 部署入口：沿用现有 `deploy/docker-compose.prod.yml` 发布流程。
- 部署步骤：后端服务重启后，前端静态资源更新并清理浏览器缓存。
- 测试与验证：
  - 教师账号（招生岗）登录并校验学生刷题信息可见且范围正确。
  - 教师账号（教学岗）登录并校验教学菜单可见且无招生越权。
  - 双岗账号登录并校验招生+教学信息同时可见。
  - 超管端开通岗位后，教师端重新登录立即生效。

## 配置与环境差异核对

- 环境：dev/test/prod 使用同一权限键，不新增环境专属权限映射。
- 配置：本次不新增 `.env` 键，不修改现有密钥配置。
- 接口影响：不新增后端写接口，相关读取接口保持向后兼容。
- 数据库：不涉及数据库 schema 变更。

## 风险说明与回滚线索

- 风险说明：岗位与权限配置错误可能导致菜单可见性偏差。
- 止血动作：超管端可立即回切 `postTags` 与权限集合。
- 回滚线索：回退到上一个稳定版本并恢复上一版角色配置快照。
